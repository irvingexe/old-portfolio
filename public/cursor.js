/*
let cursor = document.getElementsByClassName("cursor");

cursorTracking = (e) => {
    for(let i in cursor){
        console.log(cursor[i])
        cursor[i].style.left = e.clientX;
        cursor[i].style.top = e.clientY;
    }
}
*/